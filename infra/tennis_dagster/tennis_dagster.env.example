# Environment file for the tennis_dagster stack ONLY.
# This stack is intentionally isolated from any other Dagster/Postgres stacks.
#
# Usage:
#   cp infra/tennis_dagster/tennis_dagster.env.example infra/tennis_dagster/tennis_dagster.env

# Host ports (non-default to avoid conflicts with existing stacks)
DAGSTER_WEB_PORT=3001
POSTGRES_PORT=5433

# Postgres (app data DB used by scrapers; not required for Dagster internal storage)
POSTGRES_DB=tennis_simulator
POSTGRES_USER=tennis
POSTGRES_PASSWORD=tennis

# SQLAlchemy-style URL used by scraper code (inside docker-compose: host is `postgres`)
DATABASE_URL=postgresql+psycopg2://tennis:tennis@postgres:5432/tennis_simulator

# TennisAbstract sources (set these to the pages you manually verify work).
# We intentionally keep these configurable because TennisAbstract URLs/layout can change.
TA_ELO_MEN_URL=
TA_ELO_WOMEN_URL=
TA_YELO_MEN_URL=
TA_YELO_WOMEN_URL=

# Optional: a clear UA helps avoid being blocked
SCRAPER_USER_AGENT="tennis_simulator_scraper/0.1 (+contact:you@example.com)"

# Example draw page (Singles Forecast is embedded as JS variable strings on these pages)
TA_DRAW_EXAMPLE_URL=


