[project]
name = "tennis_scraper"
version = "0.1.0"
description = "Dagster code location for tennis_simulator scraping jobs"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]


